name: Clean Repo

on:
  workflow_dispatch:

jobs:
  delete_files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Delete all files
        run: |
          git rm -r *
          git commit -m "Delete all files"
          git push
